defaults:
  - _self_
  - shared

  # Comment
  # - table_wise
  # - standard
  - standard

results:
  path_to_benchmark_dataframe: "./raha/benchmark-results/benchmarks.csv"
  path_to_experiment_results_folder: "./results"
  labeling_budget: [2, 3, 5, 10, 15, 20]
  n_columns: 975
  variant: "standard" # standard, table_wise (for rt), column_wise (for XLPC)