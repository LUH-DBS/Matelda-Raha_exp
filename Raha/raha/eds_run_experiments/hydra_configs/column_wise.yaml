defaults:
  - _self_
  - shared

experiment:
  labeling_budgets: [0.10, 0.25, 0.5, 0.75, 1, 2, 3, 5, 10, 15, 20]
  n_columns: 1343
  labels_per_column: [2, 20] # determines the maximum amount of labels per column -> if changed, for example to [19] then Raha - 19LPC would be executed

logs:
  path_to_log_file: "./logs/raha-2LPC-20LPC.log"